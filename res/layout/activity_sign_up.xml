<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".SignUpActivity" >

    <EditText
        android:id="@+id/usernameUp"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:ems="10"
        android:hint="Saisir utilisateur" />

    <Button
        android:id="@+id/signup"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/passwordUp"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/passwordUp"
        android:layout_marginBottom="124dp"
        android:onClick="SignUp"
        android:text="S&apos;inscrire" />

    <EditText
        android:id="@+id/confirmUp"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/signup"
        android:layout_alignLeft="@+id/signup"
        android:layout_marginBottom="43dp"
        android:ems="10"
        android:hint="Confirmer mot de passe"
        android:inputType="textPassword" >

        <requestFocus />
    </EditText>

    <EditText
        android:id="@+id/passwordUp"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/usernameUp"
        android:layout_below="@+id/usernameUp"
        android:layout_marginTop="45dp"
        android:ems="10"
        android:hint="Saisir mot de passe"
        android:inputType="textPassword" />

</RelativeLayout>